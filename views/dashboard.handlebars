<main class="container mt-5">
  <div class="row">
    <div class="col-12">
      <h1 class="mb-4 text-black">Dashboard</h1>
      <a href="/newpost" class="btn btn-primary">Create a New Blog Post</a>
    </div>
  </div>
  <br>
  <h1 class="mb-4 text-black">Skin Script Contributions:</h1>
  {{#each posts}}
    <div class="post-container post mb-3">
      <h2><a href="/post/{{id}}" class="text-decoration-none text-reset custom-text-color" style="font-style: italic;">
        {{title}}
      </a></h2>
      <p class="text-dark">Created by: {{user.username}} | Date: {{format_date createdAt}}</p>
      <a href="/editpost/{{id}}" class="btn bg-custom">Edit Post</a>
      <button class="btn btn-danger delete-post" data-post-id="{{id}}">Delete</button>
    </div>
  {{/each}}
</main>
